# 순수익 학습 개선 — 2026-09-10

현재 실행의 관망 검증·음수 보상·가치 오차와 긴 평가 시간을 분석한 뒤 구현했다. 목표는 실제 체결, 수수료와 세금을 반영한 검증 순수익이다. 변경 후 재학습 수익률이나 A100 속도 향상은 아직 측정하지 않았다.

## 새 학습 실행

업데이트된 프로젝트 소스를 Colab에 반영한 뒤 `ai_trader/grpo/colab_train_xlstm_return_priority.ipynb`의 설정 셀부터 실행한다.

- `MODE='new'`, `LOAD_POLICY=''`
- 새 기본 폴더: `scalping_v4_account_closeout_20260910_run01`
- 기존 실행 폴더를 재사용하지 않는다. 노트북도 비어 있지 않은 새 실행 폴더를 거부한다.
- 원시 NPZ/호가 데이터는 그대로 사용할 수 있다. 새 관측 필드는 환경이 계좌·주문 상태에서 생성한다.
- GPU 사전 점검이 실제 53차원 관측으로 실행된다. 모델 폭과 비용 가정은 자동으로 줄이지 않는다.

CLI의 새 기본값은 `account_observations=True`, `liquidation_max_steps=300`, `evaluation_workers=8`, `diagnostics_interval=5`, `diagnostics_max_samples=256`이다. 수익률 우선 노트북은 검증 환경 수를 학습 worker 수에 맞춰 최대 8개로 잡는다. 기존 일반/Astral 노트북 3개는 기존 실험을 재현하도록 계좌 관측 비활성화·청산 추가 구간 0을 명시한다.

## 학습과 체결

### 관측 스키마

스키마 v3는 27개 시장 특성, 11개 계좌 필드, 기존 15개 단계 필드로 총 53차원이다. 현금 비중, 보유/단계별 평가액 비중, 미체결 매수·매도, 청산 상태, 남은 정책 결정 비율을 제공한다. 이전에는 1주와 100주 보유가 같은 관측이었지만 새 형식에서는 구분한다.

기존 v2 체크포인트와 추론 호출은 유지한다. 기존 체크포인트를 재개하면 관측 형식과 저장된 청산 설정을 복원하므로, 새 관측을 사용하려면 새 학습이 필요하다. v3 실시간 추론에는 실제 `account_state`를 제공해야 한다. 필드를 생략하거나 시장 데이터만으로 계좌를 가정하지 않는다. 계좌 이력이 없는 기존 원시 데이터 기반 BC는 v2로 유지하며 v3 BC 요청은 명확한 오류로 처리한다.

### 정책 결정 종료 후 청산

새 모드에서는 최대 300개 정책 결정 후 실제 후속 시장 이벤트를 최대 300개 더 재생해 청산한다. 300이벤트는 300초를 의미하지 않는다. 주문 지연, 부분 체결, 주문 만료와 재요청, 매수 취소 중 추가 체결도 기존 체결 시뮬레이터로 처리한다.

후속 구간의 순자산 변화와 실제 비용을 마지막 정책 보상에 합산한다. 정책 행동 수와 청산 시장 이벤트 수는 따로 기록한다. 원본 데이터나 추가 구간 예산이 부족하면 잔여 재고가 남을 수 있으며, `flat`, `step_limit`, `end_of_data`, `disabled`로 종료 사유를 남긴다. 미청산 후보는 기존과 같이 best 선정에서 제외한다. 남은 재고의 평가는 여전히 최우선 매수호가 기준이므로, 이를 실제 청산 수익과 혼동하지 않아야 한다.

### 거래 지표

기존 `num_trades`는 FIFO 매도 체결 조각 수다. 기존 제한의 의미를 보존하고 `fill_count`, `fill_win_rate`, `fill_avg_holding_time`으로 명시한다. 완료된 진입 주문별 왕복 거래를 별도로 집계하는 `round_trip_count`, `round_trip_win_rate`, 수량 가중 보유시간을 추가했다. 총 진입/청산 비용과 실현 비용 차감 전후 손익도 기록한다. 비용 필드에는 설정된 수수료·세금이 함께 포함된다.

추가 청산 구간의 실제 이벤트별 NAV를 최대낙폭과 비연율화 Sharpe 진단에 반영한다. 학습 보상은 정책 결정별 배열을 유지한다.

## 업데이트와 검증

- 수집 시 행동·로그 확률·가치 예측을 한 번에 계산한다. 가치 예측을 GAE에서 재사용한다.
- 참조 정책을 복사해 전체 데이터를 재추론하는 작업을 제거하고 저장된 rollout 로그 확률을 사용한다.
- 그룹 정책 진단은 고정 표본으로 별도 주기에 수행하며 표본 수와 비율을 기록한다. `no_grad`에서는 gradient checkpoint 분기를 건너뛴다.
- KL 기준 초과를 다음 optimizer step 전에 검사한다. 비정상 loss/gradient는 숨기지 않고 중단한다.
- 예측 가치·목표 리턴 평균/표준편차, explained variance, 클리핑 전 gradient norm, optimizer step 수를 남긴다. 목표 분산이 0이면 explained variance의 이용 불가 플래그를 함께 기록한다.
- validation은 여러 독립 환경의 관측을 묶어 추론한다. seed+에피소드 번호, 결과 순서, 비용·청산 설정, argmax 행동 선택을 유지한다.
- 동률·악화된 검증도 `checkpoints/validation/iteration_000010.json`처럼 매번 저장한다. 초기 가중치와 resume 후보 평가도 별도 이름으로 보존한다.
- TensorBoard에 행동 확률·비율, 주문/체결, 무거래 비율, 왕복 거래, 비용과 청산 진단을 기록한다. rollout·업데이트·진단·체크포인트·검증 시간을 분리한다.

검증 순수익 선정 기준, 날짜 분리, 미청산 후보 제외, 테스트를 최종 평가에만 사용하는 규칙은 유지한다. 거래를 강제하는 보너스·무거래 벌점을 추가하지 않았다. 구형 best를 새 청산 조건에 재검증 없이 승계하지 않도록 평가 서명에 청산 설정을 포함했다.

## 검증 범위와 다음 판단

최종 전체 테스트 **252개 통과**(56.28초). 실행 명령은 `.venv64/Scripts/python.exe -B -m pytest tests -o addopts='' -q --basetemp=.test_artifacts/profit_v4_final -o cache_dir=.test_artifacts/profit_v4_final_cache`이다. 변경 파일의 `git diff --check`도 통과했다.

단위·통합 테스트로 기존 체크포인트/회계/주문 처리/학습 재개를 확인했다. 실제 NPZ 환경에서 단일 평가와 배치 평가의 경로·행동·손익·진단이 일치하며, 수량 구분, 부분 체결 왕복 집계, 지연/잔량/데이터 끝의 청산, 캐시된 가치와 재계산 업데이트 일치를 검증했다. 로컬 RTX 2080 Ti에서도 작은 v3 정책의 확률 일치·마스크·역전파·유한 gradient 검사를 수행했다.

신규 학습의 수익성은 별도 확인이 필요하다. 검증 순수익과 함께 거래가 있는 에피소드 수, 왕복 거래별 손익, 실제 비용, 미청산 비율을 확인한다. 새 모델과 기존 모델을 비교할 때는 같은 데이터 경로·청산 구간·비용 조건으로 재평가해야 한다. 진단용 작은 GPU 테스트는 A100 전체 모델 학습 성능 측정을 대신하지 않는다.
