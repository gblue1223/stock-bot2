# 실행 가능한 행동과 수익 검증을 위한 첫 수정 묶음

v5 실행은 15·20·25·30회차 검증이 모두 무거래였고, 같은 검증 경로의 평균 매수 확률은 37.4%에서 13.3%로 감소했다. 학습 손실도 거래 횟수와 함께 줄었지만 거래당 실현 손실은 약 3,800원이었다. 이번 변경은 수익 개선을 검증할 실험 조건과 학습·실행의 일관성을 마련한다. 실제 A100 재학습 수익률은 아직 측정하지 않았다.

## Colab 실행과 비교 순서

`ai_trader/grpo/colab_train_xlstm_return_priority.ipynb` 전체를 최신 버전으로 다시 열고 런타임을 재시작한다. 저장소를 pull해도 이미 열린 Colab 셀은 자동 갱신되지 않는다.

```python
MODE = 'new'
LOAD_POLICY = ''
RUN_NAME = 'scalping_v6_timed_decisions_20260911_run01'
EXPERIMENT = 'timed_decisions'
```

| EXPERIMENT | 판단 간격 | 시간 상한 | 그룹 계수 | GAE lambda |
|---|---:|---:|---:|---:|
| timed_decisions | 1초 | 300초 | 1.0 | 0.95 |
| timed_gae_only | 1초 | 300초 | 0.0 | 0.95 |
| timed_long_credit | 1초 | 300초 | 0.0 | 0.99 |
| timed_monte_carlo_credit | 1초 | 300초 | 0.0 | 1.0 |

위 세 번째까지가 우선 비교 순서다. 마지막은 높은 분산을 감수하는 진단용이다. 각 실험은 새 RUN_NAME을 사용한다. 기존 `gae_only`, `long_credit` 등은 시간 간격이 없는 기준 설정에 대한 독립 실험이며 누적 적용되지 않는다. 시간 간격 외에도 정책 판단 횟수 상한과 데이터 끝이 종료 조건이다. 변경 전 실행과 비교하려면 같은 시장 경로·평가 기간·비용 조건으로 재평가한다.

주력 노트북의 공통 설정은 `execution_action_mask=True`, `no_trade_patience=3`, `profitable_min_round_trips=20`, `profitable_min_traded_dates=3`이다. CLI에도 같은 이름의 옵션을 추가했다. 일반 TrainingConfig의 조기 종료 기본값은 0(비활성)이며, 기존 노트북 세 개는 이전 동작을 기본으로 유지한다.

## 관측 63차원과 행동 마스크

관측 스키마 v4와 63차원은 그대로 유지한다. 모델 입력 차원을 늘리지 않고 `(HOLD, BUY, SELL)` 불리언 마스크를 별도로 전달한다.

- 미체결 매수는 진입 자리를 예약하고, 부분 체결과 같은 주문 ID는 중복 계산하지 않는다.
- 청산 진행·이미 도래한 위험 청산·미체결 매도·현금 부족·거래 횟수 제한을 반영한다.
- HOLD는 항상 허용한다. 손절과 보유시간 종료는 환경의 위험 관리자가 계속 수행한다.
- 롤아웃에 행동 선택 당시의 마스크를 저장하며 PPO 업데이트와 진단도 같은 마스크를 사용한다.
- GPU 사전 점검과 검증은 해당 관측을 생성한 환경의 마스크를 사용한다.

마스크는 현재 주문 제출 가능성을 나타내며 미래 호가에서의 체결을 보장하지 않는다. 실제 체결·수수료·청산 기준 NAV 보상은 유지한다.

마스크를 사용하는 모델의 실시간 추론은 기존 `account_state`, `execution_state` 외에 명시적인 `action_mask_state`를 필요로 한다. 공용 함수 `lib.action_masks.executable_action_mask`의 필드는 다음과 같다.

| 필드 | 의미 |
|---|---|
| filled_stages | 확정된 보유 단계 수 |
| occupied_stages | 보유와 미체결 매수를 합친 서로 다른 진입 주문 ID 수 |
| pending_buy / pending_sell | 현재 미체결 매수·매도 존재 여부 |
| exit_active | 청산 진행 중이거나 현재 시각에 위험 청산이 도래했는지 |
| within_trade_limit | 설정된 거래 횟수 제한 안인지 |
| buy_budget | 가용 현금과 단계별 예산 중 작은 금액, 원 단위 |
| buy_unit_cost | 현재 호가·슬리피지·틱 반올림·진입 수수료를 포함한 1주 예상 비용, 원 단위 |

새 모델에서 이 상태가 누락되면 추정값으로 대체하지 않고 오류를 낸다. 기존 체크포인트에 `execution_action_mask`가 없으면 False로 복원한다. 마스크 사용 여부는 검증 서명에도 포함되어 서로 다른 행동 조건의 best 점수를 계승하지 않는다. 가중치의 텐서 형태가 같더라도 비교 실험은 새 실행으로 시작한다.

## 무거래 조기 종료

검증에서 거래 체결이 없고 무거래 비율 100%, 수익률 0%이며 매수 확률도 확인 가능할 때만 판정한다. 순수익이 개선되지 않고 매수 확률이 이전 무거래 검증보다 증가하지 않는 상태가 `no_trade_patience`회 연속이면 `no_trade_collapse`로 종료한다. 증거가 없거나 매매가 재개되면 연속 횟수를 초기화한다.

최초 0% 검증이 이전 손실보다 개선된 경우에는 그 회차를 정체 횟수에 넣지 않는다. 따라서 v5 같은 15회차 최초 무거래 → 20·25·30회차 정체는 기본값 3에서 30회차 종료에 해당한다. 체크포인트는 업데이트 후 검증 전에 저장하고, 검증 뒤에는 최신 제어 상태로 갱신한다. 종료 사유와 실제 완료 반복·스텝을 보고한다. 재개 시 동일한 평가·제어 조건의 상태만 이어받는다.

재개 CLI에서 `--no_trade_patience`, `--profitable_min_round_trips`, `--profitable_min_traded_dates`를 명시하면 해당 제어 설정을 바꿀 수 있다. 설정이 바뀌면 이전 연속 무거래 기록을 초기화하고 실제 적용값을 설정 JSON에도 저장한다. 예를 들어 `--no_trade_patience 0`은 조기 종료를 끈다.

조기 종료한 실행에서는 테스트 기간을 열지 않는다. `evaluation_report.json`의 `test`는 null이고 `test_skip_reason`에 이유를 저장한다. Colab 결과 표는 이 경우도 표시할 수 있다.

## 수익 후보 체크포인트

`checkpoint_best.pt`는 기존처럼 검증 순수익이 가장 높은 모델이며, 0% 무거래 모델일 수 있다. 별도의 `checkpoint_best_profitable.pt`는 다음 근거를 확인한 후보만 저장한다.

- 검증 평균 순수익률이 양수이고 실제 체결이 있다.
- 모든 에피소드가 완전히 청산됐으며 잔고가 없다.
- 최소 왕복 거래 수와 서로 다른 거래 날짜 수를 충족한다(주력 노트북 기본값 20건·3일).
- 에피소드별 체결·왕복 횟수·청산 정보가 합계와 일치하며 필요한 진단이 누락되지 않았다.

후보 파일의 존재는 수익 보장이나 실거래 승인 기준이 아니다. 날짜별 편중·추가 시드·체결 비용 변화와 최종 테스트 평가는 별도로 확인해야 한다. resume 후보 탐색은 두 종류 best를 모두 검사하고 현재 검증 조건으로 재평가한다.

현재 검증으로 인정되는 후보가 없는데 이전 수익 후보 파일이 남아 있으면 `checkpoint_unqualified_profitable_<iteration>[_N].pt`로 보관한다. 기존 보관 파일은 덮어쓰지 않는다.

## 적용 범위

이번 커밋은 추천한 첫 수정 묶음이다. 같은 경로의 GRPO 그룹 수집, 수량별 호가 비용 추정·주문 크기 실험, 수익 예측 보조 학습은 포함하지 않는다. 시간 조건을 고정한 GAE 단독 비교 결과와 순수익 기회 분석을 근거로 다음 단계에서 검증한다.

## 검증

설정·CLI·구버전 복원, Colab 복합 프리셋과 GPU 마스크 전달, 미체결·현금·위험 청산, 마스크를 저장한 likelihood 재계산, CPU/CUDA의 63차원 수집·업데이트·검증, 조기 종료·재개 상태·수익 후보 선정·최종 테스트 접근 여부를 검증했다. 최종 전체 회귀 테스트는 **470 passed in 55.89s**이며 `git diff --check`도 통과했다. 실행 명령은 `.venv64/Scripts/python.exe -B -m pytest tests -q -o addopts='' --basetemp=.test_artifacts/profit_controls_final -o cache_dir=.test_artifacts/pytest_cache`이다.
