# xLSTM 업데이트 검증과 연속 무거래 상한

`scalping_v5_execution_cost_20260912_run02`는 그룹 상대 보상을 제거해도 검증 8회 모두 무거래로 끝났다. 19회차의 샘플 KL 추정치가 0.912715까지 상승했고, 20~32회차는 실제 optimizer 실행이 회차당 1~6회에 그쳤다. 이 변경은 업데이트 전후의 확률을 확인하고, 한계를 넘은 업데이트를 되돌리며, 실제 거래가 없는 실행의 검증 횟수를 제한한다.

## 업데이트 전후 검사

- 수집한 모든 관측·행동·실행 마스크를 CPU에 유지하고 미니배치 단위로 학습 forward를 다시 실행한다. 수집 당시 선택 행동의 log probability와 비교해 최대 절대 오차가 허용값보다 크면 optimizer를 실행하기 전에 오류로 중단한다.
- 검사 중에는 실제 학습 모드와 gradient checkpointing 경로를 사용한다. 학습 forward가 BatchNorm 등의 버퍼를 변경하면 오류로 처리하고 버퍼를 복원한다.
- 원정책의 전체 행동 log probability는 작은 `관측 수 × 행동 수` CPU 배열로 저장한다. 별도 GPU 참조 모델은 만들지 않는다.
- optimizer 실행 전에는 현재 미니배치의 전체 행동 분포로 정확한 `KL(원정책 || 현재 정책)`의 평균을 계산해 `kl_target * 1.5`와 비교한다. 이미 수행한 학습 forward의 분포를 재사용하므로 추가 모델 forward는 없다. 선택된 행동만으로 추정한 sampled KL은 작은 배치에서 과대·과소 추정될 수 있어, 검사가 활성화된 경우 진단값으로만 기록한다.
- optimizer 실행 후 새 forward로 현재 미니배치와 고정 관측 표본의 `KL(원정책 || 새 정책)`을 계산한다. 각 관측에서 세 행동을 모두 합산하고, 각각의 표본 평균을 기존 `kl_target * 1.5` 한계와 비교한다. 고정 표본은 전체 상태 공간을 보장하는 검사가 아니다.
- 한계를 넘은 단계는 직전 모델 가중치·버퍼·Adam 상태를 함께 복원하고 해당 회차의 나머지 업데이트를 중단한다. 실패한 단계를 승인된 optimizer 실행 횟수와 평균 손실에 포함하지 않는다. 앞서 승인된 단계는 유지한다.
- 비정상 값이나 검사 예외도 업데이트 후 복원 경로를 거친다. 학습률이나 거래 보상은 이 기능이 자동 변경하지 않는다.

## 설정

| 설정 | 새 학습 `TrainingConfig` 기본값 | 주 Colab 노트북 기본값 | 의미 |
|---|---:|---:|---|
| `policy_update_checks` | `True` | `True` | 수집·학습 확률 일치 및 업데이트 전후 정확한 KL 검사 |
| `rollout_logprob_tolerance` | `0.001` | `0.001` | 수집·학습 log probability의 최대 절대 오차 허용값 |
| `kl_probe_samples` | `32` | `32` | 각 업데이트 후 추가 검사하는 고정 관측 표본 수 |
| `no_trade_max_validations` | `0` | `4` | 확률 변화와 무관한 연속 무거래 검증 상한, 0은 비활성 |

직접 `GRPOTrainer`를 사용하는 기존 커스텀 정책과의 호환을 위해 생성자의 `policy_update_checks` 기본값은 `False`다. 검사 활성화 정책은 `evaluate_actions_with_distribution()`을 제공해야 한다. xLSTM 정책은 이 메서드를 제공하며 관측 스키마나 가중치 크기는 변경되지 않는다.

검사를 끈 기존 정책은 sampled KL 중단을 유지한다. 검사를 켰는데 전체 행동 분포가 없거나 유효하지 않으면 기존과 같이 오류로 중단한다. 정확한 KL로의 변경에는 새 CLI나 하이퍼파라미터가 필요 없으며, `policy_update_checks=True`인 기존 체크포인트에도 코드 갱신 후 적용된다. 실행 후 고정 표본·미니배치 검사와 Adam 복원, 기존 KL 한도는 유지된다.

업데이트 지표 `pre_update_kl_uses_exact`는 실제 사전 중단 기준을 표시한다. `last_checked_kl`은 그 기준의 마지막 값이며, `last_sampled_kl`은 선택 행동 기반 추정값이다. `pre_update_minibatch_exact_kl`은 마지막 사전 검사의 정확한 평균이고 `pre_update_exact_kl_checked`는 계산 여부를 뜻한다. `pre_update_sampled_only_exceedances`는 sampled만 한도를 초과한 배치 수, `pre_update_exact_only_exceedances`는 정확한 KL만 한도를 초과한 배치 수다. `kl_divergence`는 호환성을 위해 수락된 배치의 기존 sampled KL 평균을 유지한다.

기존 `no_trade_patience=3`도 유지한다. 이는 수익 비개선과 매수 확률 비증가를 함께 보는 기존 조건이다. 새 `no_trade_max_validations`는 검증된 무거래를 첫 검증부터 세며, 매수 확률이 상승하거나 확률 진단이 없어도 무체결·수익 0·완전 청산이 확인되면 센다. 실제 거래 발생이나 불완전한 증거는 연속 기록을 초기화한다. 두 조건이 동시에 충족되면 기존 `no_trade_collapse` 사유를 우선하고, 새 상한만 충족하면 `no_trade_limit`로 종료한다.

예를 들어 검증 간격이 5이고 새 상한이 4이면, 검증 5·10·15·20이 모두 무거래일 때 20회차까지 종료한다. 매수 확률의 반등으로 이 상한이 연장되지 않는다. 손실 거래를 하는 실행을 이 상한으로 중단하지는 않는다.

체크포인트에는 설정과 두 종류의 연속 기록을 저장한다. 기존 체크포인트에 없던 검사와 상한은 재개 시 비활성으로 해석한다. CLI에서 명시적으로 지정한 설정은 재개에 반영하며, 실제 적용값을 `training_config.json`에도 저장한다. 새 상한의 과거 기록은 기존 확률 기반 카운터에서 추정하지 않는다.

## Colab 적용

주 노트북은 `ai_trader/grpo/colab_train_xlstm_return_priority.ipynb`다.

1. 수정된 노트북을 열고 **3. 수정된 프로젝트 코드 준비**에서 `REVISION='dev-tf-rl'`로 코드를 받는다. 이미 열려 있는 Colab 셀은 git 갱신으로 자동 교체되지 않으므로 새 노트북의 셀을 사용한다. 기존 런타임에서 코드를 갱신했다면 이전 Python 모듈이 메모리에 남지 않도록 런타임을 재시작하고 1번부터 순서대로 다시 실행한다.
2. **5. A100 프로필 및 실험 설정**에서 새 `RUN_NAME`, `MODE='new'`, `EXPERIMENT='timed_gae_only'`, `LOAD_POLICY=''`를 사용한다. 1초/300초와 `lambda_gae=0.95`를 유지하여 업데이트 검증의 영향을 먼저 확인한다.
3. **6. 날짜 분리 및 관측/체결 사전 검사**를 실행한다.
4. **7. GPU 배치 사전 점검**에서 실제 관측과 실행 마스크로 수집 배치와 학습 배치의 확률 일치를 확인한다. 정책 헤드를 0으로 만들지 않는다. 5번 설정 셀의 `DIAGNOSTIC_CHECKPOINT`를 지정하면 저장된 가중치도 별도로 확인하며, 이 경로는 학습용 `LOAD_POLICY`와 구분된다.
5. 검사를 통과하면 **8. 학습 시작**을 실행한다. 설정을 바꾸면 GPU 사전 점검도 다시 실행한다.

학습은 별도 Python 프로세스에서 실행되므로 노트북 커널에서만 함수나 클래스를 재정의하는 방식으로는 적용되지 않는다. 저장소의 실제 `.py` 파일과 새 노트북을 함께 사용해야 한다.

검증 forward와 CPU 상태 복사가 추가되므로 회차당 계산 비용이 증가한다. 기존 3초대 업데이트는 대부분의 최적화가 차단된 상태였으므로 비교 기준으로 삼지 않는다.

## 실제 실행 자료로 확인한 범위

로컬 `data/extracted_episodes_v2/manifest.json`의 SHA256은 Colab 실행과 같은 `416dcc932c0685adc71d9ee1aa3620f470c278d568dd8543302631f97949f2a4`다. 해당 학습 데이터에서 수집한 32개 `1024 × 63` 관측과 세 종류의 마스크(HOLD만 가능, BUY 가능, SELL 가능)를 사용했다.

run02의 18·19회차 실제 체크포인트를 로드해 수집 배치 8과 학습 배치 32를 비교했으며, 두 체크포인트 모두 선택 행동 log probability 최대 절대 오차와 변경 전 전체 행동 KL이 0이었다. 정책 헤드는 실제 저장 가중치를 유지했다.

이 확인은 RTX 2080 Ti와 PyTorch 2.8.0+cu126에서 수행했다. 원래 A100/PyTorch 2.11.0 환경의 TF32 연산을 재현한 결과가 아니므로 Colab의 새 GPU 사전 점검으로 해당 환경을 확인해야 한다. 이 결과만으로 원래 KL 급등의 원인을 확정하거나 수익성 향상을 주장하지 않는다.

## 회귀 검사

2026-09-12 전체 테스트 **533개가 통과**했다(52.28초). CPU와 CUDA에서 실행 가능한 실제 63차원 정책 업데이트, 확률 불일치 시 optimizer 실행 방지, KL 초과 및 비정상 값 발생 시 초기화된 Adam·모델·버퍼 복원, 연속 무거래 상한과 재개 상태, Colab 셀 실행 및 기존 스키마 2/3/4 호환을 포함한다.

```powershell
.\.venv64\Scripts\python.exe -m pytest --basetemp .test_artifacts/pytest_update_checks_full_02
```

사용자가 삭제한 기존 Colab 노트북 4개는 복원하지 않았으며, 노트북 테스트와 현재 실행 안내는 남은 `colab_train_xlstm_return_priority.ipynb`를 기준으로 정리했다.
