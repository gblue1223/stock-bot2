# 정규화 통계 파일 비교 결과

## 비교 대상

1. **기존 파일**: `models/grpo_scalping@2025120/normalization_stats.json`
2. **새 파일**: `C:\Users\user\Workspace\datasets@20251013\normalization_stats.pkl`

## 비교 결과: ✅ 동일함

### Mean 값 비교

| 특징 | JSON | PKL | 일치 |
|------|------|-----|------|
| 종목명_scalar | 0.6242419481 | 0.6242419481 | ✅ |
| 시간_sin | 0.5349534154 | 0.5349534154 | ✅ |
| 시간_cos | -0.8311624527 | -0.8311624527 | ✅ |
| 시간_scalar | -2.519e-15 | -2.095e-15 | ✅ (≈0) |
| 등락률 | -2.696e-14 | 1.888e-14 | ✅ (≈0) |
| ... | ... | ... | ✅ |

### Std 값 비교

| 특징 | JSON | PKL | 일치 |
|------|------|-----|------|
| 종목명_scalar | 0.05148928 | 0.05148928 | ✅ |
| 시간_sin | 0.12954657 | 0.12954657 | ✅ |
| 시간_cos | 0.07881328 | 0.07881328 | ✅ |
| 시간_scalar | 1.0 | 1.0 | ✅ |
| 등락률 | 1.0 | 1.0 | ✅ |
| ... | ... | ... | ✅ |

### 특징 목록 비교

두 파일 모두 동일한 28개 특징을 포함:
1. 종목명_scalar
2. 시간_sin
3. 시간_cos
4. 시간_scalar
5. 등락률
6. 누적거래대금
7. 거래회전율
8. 체결강도
9-18. 매도대기금액1~10
19-28. 매수대기금액1~10

## 결론

✅ **두 파일의 내용이 완전히 동일합니다.**

- Mean 값: 동일 (부동소수점 오차 범위 내)
- Std 값: 동일
- 특징 목록: 동일
- 특징 개수: 28개 (동일)

## 의미

1. **기존 JSON 파일이 이미 올바른 정규화 통계를 포함**하고 있었음
2. 새로 생성한 PKL 파일은 동일한 데이터베이스에서 생성되어 같은 결과
3. **JSON 파일을 그대로 사용 가능**

## 권장 사항

### 옵션 1: 기존 JSON 파일 사용 (권장)
```python
# config/trading_config.json에서 이미 설정되어 있음
{
    "normalization_stats_path": "models/grpo_scalping@2025120/normalization_stats.json"
}
```

### 옵션 2: PKL 파일로 교체
```bash
# 필요시 PKL 파일을 모델 디렉토리로 복사
cp "C:\Users\user\Workspace\datasets@20251013\normalization_stats.pkl" \
   "models/grpo_scalping@2025120/normalization_stats.pkl"
```

## 다음 단계

기존 JSON 파일이 올바른 통계를 포함하고 있으므로:

1. ✅ 정규화 통계는 이미 준비됨
2. ⏭️ 실시간 거래 시스템에서 이 통계를 **실제로 사용**하도록 확인 필요
3. ⏭️ `live_trading.py`가 정규화 통계를 로드하고 적용하는지 확인

## 핵심 문제

정규화 통계 파일은 존재하고 올바르지만, **실시간 추론 시 이 통계가 사용되지 않고 있을 가능성**이 높습니다.

확인 필요:
- `OnlineNormalizer`가 학습 데이터 통계를 사용하는가?
- 아니면 실시간 데이터만으로 통계를 계산하는가?

→ 이것이 Buy Signal Rate 0%의 근본 원인일 수 있습니다.
